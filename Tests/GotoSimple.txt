// Prueba de GoTo con condiciones
Spawn(10, 10)
Color("Black")
Size(2)

// Variables
a <- 5
b <- 3
contador <- 0

// Etiqueta de inicio del bucle
inicio
contador <- contador + 1
Color("Red")
DrawLine(1, 0, contador)  // Dibujar línea horizontal de longitud actual

// Salto condicional - repetir mientras contador < 3
GoTo [inicio] (contador < 3)

// Al salir del bucle, dibujar un círculo
Color("Blue")
Size(contador)
DrawCircle(5, 5, 1)
