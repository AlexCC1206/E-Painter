// Prueba simplificada de operaciones
Spawn(10, 10)
Color("Black")
Size(2)

// Variables para pruebas
a <- 5
b <- 3
c <- 5

// Operaciones aritméticas
suma <- a + b
resta <- a - b
multi <- a * b
div <- a / b
modulo <- a % b

// Usar resultados
DrawLine(1, 0, suma)   // Línea horizontal de longitud 8
DrawLine(0, 1, resta)  // Línea vertical de longitud 2
Size(resta)            // Cambiar tamaño a 2
