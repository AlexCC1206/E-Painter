// Prueba simple de funciones del sistema
Spawn(10, 10)
Color("Black")
Size(3)

// Dibujar algo simple para ver en el canvas
DrawCircle(0, 0, 3)

// Prueba de ColoresValidos
Color("Red")
DrawLine(1, 0, 3)
Color("Green")
DrawLine(0, 1, 3)
Color("Blue")
DrawCircle(3, 3, 1)

// Prueba de OperacionesNuméricas
a <- 5
b <- 3
suma <- a + b       // 8
resta <- a - b      // 2
multi <- a * b      // 15
div <- a / b        // 1
modulo <- a % b     // 2

// Usar resultados en comandos - Sólo podemos usar 1, 0 o -1 como dirección de línea
DrawLine(1, 0, suma)     // Dibuja línea horizontal de longitud 8
DrawLine(0, 1, resta)    // Dibuja línea vertical de longitud 2
Size(resta)              // Cambia tamaño a 2
