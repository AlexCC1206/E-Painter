// Prueba básica para verificar funcionalidades
Spawn(10, 10)
Color("Black")
Size(2)

// Dibujar un cuadrado
DrawLine(1, 0, 5)
DrawLine(0, 1, 5)
DrawLine(-1, 0, 5)
DrawLine(0, -1, 5)

// Dibujar un círculo
Color("Blue")
DrawCircle(0, 0, 3)
